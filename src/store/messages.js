// Modelos de mensajes
const state = {
  messages: [],
};

const getters = {};

const mutations = {};

const actions = {};

export default {
  // Importante namespace para evitar problemas de importanción
  namespaced: true,
  state,
  getters,
  actions,
  mutations,
};
